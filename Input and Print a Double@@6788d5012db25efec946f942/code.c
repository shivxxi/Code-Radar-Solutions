#include <stdio.h>

int main()
{
    double number;
    printf("");
    //fgets(input,sizeof(input),stdin);
    scanf("%lf",&number);
    printf("You entered: %lf",number);
    return 0;
    
}