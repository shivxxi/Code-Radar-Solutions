#include <stdio.h>

int main()
{
    char number[100];
    printf("");
    fgets(input,sizeof(input),stdin);
    // scanf("%lf",&number);
    printf("You entered: %s",number);
    return 0;
    
}